# Atividade 4 — Do código-fonte ao executável
# Preencha este arquivo durante a atividade.
# ─────────────────────────────────────────────────────

# FASE 1 — Pré-processador (gcc -E)
# Quantas linhas tem soma.i? O que o pré-processador expandiu?

Linhas em soma.i: 757 soma.i
Observação: O pré-processador expandiu principalmente o conteúdo da biblioteca, com definições, macros, tipos e declarações usadas pela biblioteca stdio.h. 


# FASE 2 — Assembly (gcc -S)
# Cole aqui 5 a 10 linhas de soma.s que você considera mais
# interessantes.

soma:
        addi    sp,sp,-32
        sd      s0,24(sp)
        addi    s0,sp,32
        mv      a5,a0
        mv      a4,a1
        sw      a5,-20(s0)
        mv      a5,a4
        sw      a5,-24(s0)
        lw      a4,-20(s0)
        lw      a5,-24(s0)
        addw    a5,a4,a5
        sext.w  a5,a5
        mv      a0,a5
        ld      s0,24(sp)
        addi    sp,sp,32
        jr      ra
        .size   soma, .-soma
        .section        .rodata
        .align  3

Trecho de soma.s: 54 soma.s

# FASE 3 — Código objeto (gcc -c)
# Cole a saída de: file soma.o

file soma.o: soma.o: ELF 64-bit LSB relocatable, UCB RISC-V, version 1 (SYSV), not stripped

# Cole a saída de: nm soma.o

nm soma.o:

0000000000000000 r .LC0
0000000000000028 T main
                 U printf
0000000000000000 T soma

# Quais símbolos aparecem? O que significa o marcador "U" ao lado
# de printf?

R: O marcador "U" significa que ele foi usado mas não definido nesse arquivo.

Observação sobre nm: 

R: Os símbolos "soma" e "main" aparecem marcados com "T", indicando que
estão definidos na seção de texto.


# FASE 4 — Ligação (gcc soma.o -o soma)
# O que mudou após a ligação? 

R: O arquivo objeto soma.o foi combinado com as bibliotecas necessárias, gerando um executável final.

#Execute ./soma e registre a saída.

Saída de ./soma: 12


# QUESTÕES DE SÍNTESE

# 1. O montador histórico da genealogia resolvia rótulos (labels) em endereços de memória.
# Na saída do nm soma.o, printf aparece marcado como U (Undefined). Qual é o papel do 
# ligador em relação a esse símbolo? 

Resposta 1:

R: O ligador procura a definição de printf nas bibliotecas necessárias, resolve o undefined ("U"), associando a chamada ao endereço correto no executável final.

# 2. Você gerou três arquivos intermediários: soma.i (pré-processado), soma.s (assembly),
#    soma.o (objeto). Qual desses é o que o computador realmente executa?

Resposta 2:

R: Nenhum deles são executados diretamente. O arquivo executado é o arquivo final gerado pelo somador, por exemplo: ./soma.